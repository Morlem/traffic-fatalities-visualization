- change background color behind map
- create icons for the different modes in the same style
- redesign detail pane as cards
====================================
- revisit filtering
  - top label to: Filtering
  - add a reset action
  - can click filters again to apply filtering
  - show mouse as cursor over filters
  - fix filtering for other criteria that are currently disabled
- Enable map render again
  - use the lat and longitude values that I added
  - filter people shown based on map zoom (maybe add checkbox to enable map filtering)
- HTML / CSS for detail pane
